# Fun-o-de-troca-de-valores


#include <stdio.h>
#define MAX 5


int main()
{
    int x = 10; 
    int *p; ///declara ponteiro para inteiro (está apontando) //quando o pontiro é declarado, deve ser dado o tipo
    p = &x; ///& pega o endereço da variavel
}
